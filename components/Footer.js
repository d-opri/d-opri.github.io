import styled from "styled-components";

const StyledFooter = styled.footer`
  text-align: center;
  margin: auto;
`;

export default function Footer() {
  return (
    <StyledFooter>
      <p>@DaniOpri</p>
    </StyledFooter>
  );
}
